const planet="Tierra"; // es una variable const ya que es un nombre propio y no cambia
let currentUser="Juan"; //let ya que el usuario actual puede variar en cada momento

console.log(currentUser + " esta en el planeta " + planet);
